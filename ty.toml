[environment]
# Use virtual environment python when available; ty will auto-detect if not specified

[src]
include = [
    "bio_inspired_nanochat",
    "scripts",
    "tasks",
    "tests",
]
exclude = [
    "third_party/**",
]

